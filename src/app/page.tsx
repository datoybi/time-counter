import { Nanum_Pen_Script } from "next/font/google";

const font = Nanum_Pen_Script({
  subsets: ["latin"],
  display: "swap",
});

export default function Home() {
  return <main className="flex">hi</main>;
}
